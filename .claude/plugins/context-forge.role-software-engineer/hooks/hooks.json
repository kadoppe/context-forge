{
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Python ファイルに変更があった場合、以下のコマンドを順番に実行してください:\n\n1. `uv run ruff check .` - リンターチェック\n2. `uv run mypy src` - 型チェック\n3. `uv run pytest` - テスト実行\n\nエラーが発生した場合は、エラー内容を確認して修正してください。修正後は再度チェックを実行して、すべてのチェックがパスするまで繰り返してください。"
          }
        ]
      }
    ]
  }
}
